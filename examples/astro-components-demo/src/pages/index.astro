---
import Layout from "../layouts/Layout.astro";
import { loadPayNowServiceInitialData } from "@wix/headless-stores/services";
import { ProductPage } from "../islands/ProductPage";

const payNowInitialData = await loadPayNowServiceInitialData("Sveder Mechoar", "123");
---

<Layout>
  <ProductPage client:load servicesConfigs={{ ...payNowInitialData }} />
</Layout>
